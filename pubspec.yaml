name: link_five
description: A game. First to get five in a row wins!

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  built_value: ^8.1.1
  built_collection: ^5.1.0
  firebase_core: ^2.23.0
  firebase_auth: ^4.14.1
  cloud_firestore: ^4.13.2

dev_dependencies:
  build_runner: ^2.0.6
  built_value_generator: ^8.1.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
